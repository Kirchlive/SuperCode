// test_fixtures/sample_project/utils.js

/**
 * A simple function to double a number.
 * @param {number} n The number to double.
 * @returns {number} The doubled number.
 */
export function double(n) {
    return n * 2;
}
